---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Bio from '../../components/Bio.astro';
import ShareButtons from '../../components/ShareButtons.astro';

export async function getStaticPaths() {
  const posts = import.meta.glob('../../data/blog-posts/*.md', { eager: true });

  return Object.entries(posts).map(([filePath, post]: [string, any]) => {
    const fileSlug = filePath.split('/').pop()?.replace('.md', '') || '';
    const slug = post.frontmatter?.slug || fileSlug;
    return {
      params: { slug },
      props: { post },
    };
  });
}

---
// Existing imports...
const { post } = Astro.props;
const { Content, frontmatter, rawContent } = post;

// High-CTR BreadcrumbList Schema for Google Search
const breadcrumbSchema = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    {
      "@type": "ListItem",
      "position": 1,
      "name": "Home",
      "item": "https://cyprusstudentguide.com/"
    },
    {
      "@type": "ListItem",
      "position": 2,
      "name": "Guides",
      "item": "https://cyprusstudentguide.com/blog/"
    },
    {
      "@type": "ListItem",
      "position": 3,
      "name": title,
      "item": permalink
    }
  ]
};
---

<BaseLayout title={title} description={description} permalink={permalink} current="blog">
  <script type="application/ld+json" is:inline set:html={JSON.stringify(articleSchema)} />
  <script type="application/ld+json" is:inline set:html={JSON.stringify(breadcrumbSchema)} />

  <!-- Rest of the code... -->

  <!-- Live Top Reading Progress Bar -->
  <div class="reading-progress-track">
    <div id="reading-progress-bar" class="reading-progress-fill"></div>
  </div>

  <article class="post-detail-container">
    <!-- Clean Minimalist Header Card -->
    <header class={`post-hero-card ${categoryThemeClass}`}>
      <div class="post-hero-meta-row">
        <span class="category-tag"><span class="cat-icon notranslate">{categoryIcon}</span> {category}</span>
        <span class="read-time-pill notranslate">⏱️ {readTime} min read</span>
        <time datetime={publishDate} class="date-pill notranslate">📅 {formattedDate}</time>
      </div>

      <h1 class="post-title">{title}</h1>
      
      <div class="author-row">
        <span>Verified Student Guide • By <strong>{author}</strong></span>
      </div>
    </header>

    <!-- Key Takeaways / Quick TL;DR Card -->
    <div class="tldr-box">
      <div class="tldr-header">
        <span class="tldr-badge notranslate">⚡ SUMMARY</span>
        <strong>Key Field Takeaways</strong>
      </div>
      <p>{description}</p>
    </div>

    <!-- Main Markdown Article Body -->
    <div class="post-content-body">
      <Content />
    </div>

    <!-- Smart Interactive Action Widget -->
    <div class="smart-tool-callout">
      <div class="smart-tool-icon notranslate">🛠️</div>
      <div class="smart-tool-info">
        <h3>Track Shifts & Split Courier Fuel Payouts</h3>
        <p>Log your delivery shifts (Wolt / Foody), calculate accurate 8.3% Social Insurance & 2.65% GeSY deductions, and generate audit slips.</p>
      </div>
      <a href="/shift-tracker/" class="smart-tool-btn">Open Shift Tracker ➔</a>
    </div>

    <!-- Share & Author Section -->
    <footer class="post-footer-section">
      <div class="share-box-wrap">
        <span class="share-label">Help fellow students in Cyprus:</span>
        <ShareButtons title={title} url={permalink} />
      </div>
      <Bio />
    </footer>

    <!-- Back to Hub Floating Link -->
    <div class="back-hub-wrap">
      <a href="/blog/" class="back-hub-link">← Return to All Guides & Articles</a>
    </div>
  </article>
</BaseLayout>

<script is:inline>
  window.addEventListener('scroll', () => {
    const totalHeight = document.documentElement.scrollHeight - window.innerHeight;
    const progress = (window.scrollY / totalHeight) * 100;
    const bar = document.getElementById('reading-progress-bar');
    if (bar) {
      bar.style.width = Math.min(100, Math.max(0, progress)) + '%';
    }
  });
</script>

<style>
 /* Reading Progress Track (Fixed to the very top of viewport) */
  .reading-progress-track {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 4px;
    background: transparent;
    z-index: 99999;
    pointer-events: none;
  }
  .reading-progress-fill {
    height: 100%;
    width: 0%;
    background: linear-gradient(90deg, #2563eb, #0284c7);
    transition: width 0.08s ease-out;
  }

  .post-detail-container {
    max-width: 820px;
    margin: 0 auto;
    padding: 1.5rem 0.75rem 4rem;
  }

  /* Header Card */
  .post-hero-card {
    background: var(--card-bg);
    border: 1px solid var(--card-border);
    border-radius: 20px;
    padding: 2.25rem 2rem 2rem;
    margin-bottom: 2rem;
    position: relative;
    overflow: hidden;
  }
  .post-hero-card:before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
  }
  .theme-immigration:before { background: linear-gradient(90deg, #6366f1, #8b5cf6); }
  .theme-banking:before { background: linear-gradient(90deg, #059669, #10b981); }
  .theme-jobs:before { background: linear-gradient(90deg, #d97706, #f59e0b); }
  .theme-living:before { background: linear-gradient(90deg, #0284c7, #38bdf8); }
  .theme-general:before { background: linear-gradient(90deg, #2563eb, #3b82f6); }

  .post-hero-meta-row {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
    margin-bottom: 1.25rem;
    font-size: 0.82rem;
    font-weight: 700;
  }
  .category-tag {
    background: rgba(37, 99, 235, 0.1);
    color: var(--primary-blue);
    padding: 0.25rem 0.65rem;
    border-radius: 6px;
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
  }
  .read-time-pill, .date-pill {
    color: var(--text-muted);
  }

  .post-title {
    font-size: clamp(1.8rem, 4vw, 2.5rem);
    font-weight: 900;
    line-height: 1.25;
    margin: 0 0 1rem;
    letter-spacing: -0.02em;
  }
  .author-row {
    font-size: 0.88rem;
    color: var(--text-muted);
  }
  .author-row strong {
    color: var(--text-main);
  }

  /* TL;DR Summary Box */
  .tldr-box {
    background: rgba(37, 99, 235, 0.04);
    border: 1px solid rgba(37, 99, 235, 0.2);
    border-left: 5px solid var(--primary-blue);
    border-radius: 12px;
    padding: 1.25rem 1.5rem;
    margin-bottom: 2.5rem;
  }
  .tldr-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
    font-size: 0.9rem;
  }
  .tldr-badge {
    background: var(--primary-blue);
    color: #ffffff;
    font-size: 0.7rem;
    font-weight: 900;
    padding: 0.15rem 0.45rem;
    border-radius: 4px;
    letter-spacing: 0.05em;
  }
  .tldr-box p {
    color: var(--text-main);
    font-size: 0.96rem;
    line-height: 1.6;
    margin: 0;
  }

  /* Content Body */
  .post-content-body {
    font-size: 1.05rem;
    line-height: 1.75;
    color: var(--text-main);
  }
  .post-content-body :global(h2) {
    font-size: 1.65rem;
    font-weight: 800;
    margin: 2.5rem 0 1rem;
    line-height: 1.3;
    letter-spacing: -0.01em;
    border-bottom: 1px dashed var(--card-border);
    padding-bottom: 0.5rem;
  }
  .post-content-body :global(h3) {
    font-size: 1.3rem;
    font-weight: 800;
    margin: 1.75rem 0 0.75rem;
  }
  .post-content-body :global(p) {
    margin-bottom: 1.35rem;
  }
  .post-content-body :global(ul), .post-content-body :global(ol) {
    margin: 0 0 1.5rem 1.5rem;
  }
  .post-content-body :global(li) {
    margin-bottom: 0.5rem;
  }
  .post-content-body :global(blockquote) {
    background: var(--card-bg);
    border-left: 4px solid var(--primary-blue);
    padding: 1rem 1.25rem;
    margin: 1.5rem 0;
    border-radius: 0 8px 8px 0;
    font-style: italic;
  }
  .post-content-body :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: 1.5rem 0;
    font-size: 0.95rem;
  }
  .post-content-body :global(th), .post-content-body :global(td) {
    border: 1px solid var(--card-border);
    padding: 0.75rem 1rem;
    text-align: left;
  }
  .post-content-body :global(th) {
    background: var(--card-bg);
    font-weight: 800;
  }

  /* Napkin AI Flow Grid */
  .post-content-body :global(.infographic-wrapper) {
    background: var(--card-bg);
    border: 1px solid var(--card-border);
    border-radius: 16px;
    padding: 1.75rem 1.5rem;
    margin: 2.25rem 0;
    box-shadow: 0 4px 20px rgba(0,0,0,0.02);
  }
  .post-content-body :global(.infographic-badge) {
    background: rgba(37, 99, 235, 0.1);
    color: var(--primary-blue);
    font-size: 0.75rem;
    font-weight: 800;
    padding: 0.25rem 0.65rem;
    border-radius: 20px;
    display: inline-block;
    margin-bottom: 0.5rem;
  }
  .post-content-body :global(.infographic-title) {
    font-size: 1.3rem;
    font-weight: 800;
    margin: 0 0 1.25rem 0;
    border: none;
    padding: 0;
  }
  .post-content-body :global(.napkin-flow-grid) {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1rem;
  }
  .post-content-body :global(.napkin-step-card) {
    background: rgba(150, 150, 150, 0.04);
    border: 1px solid var(--card-border);
    border-radius: 12px;
    padding: 1.15rem;
    position: relative;
  }
  .post-content-body :global(.step-num-badge) {
    width: 26px;
    height: 26px;
    background: var(--primary-blue);
    color: #ffffff;
    font-size: 0.76rem;
    font-weight: 900;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 0.6rem;
  }
  .post-content-body :global(.napkin-step-card h4) {
    font-size: 0.96rem;
    font-weight: 800;
    margin: 0 0 0.35rem 0;
  }
  .post-content-body :global(.napkin-step-card p) {
    font-size: 0.85rem;
    line-height: 1.5;
    margin: 0;
    color: var(--text-muted);
  }

  /* Napkin Stat Row */
  .post-content-body :global(.napkin-stat-row) {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
    gap: 1rem;
  }
  .post-content-body :global(.napkin-stat-box) {
    background: rgba(150, 150, 150, 0.04);
    border: 1px solid var(--card-border);
    border-radius: 12px;
    padding: 1.15rem 1rem;
    text-align: center;
  }
  .post-content-body :global(.stat-icon-wrap) { font-size: 1.75rem; margin-bottom: 0.35rem; }
  .post-content-body :global(.stat-big) {
    font-size: 1.15rem;
    font-weight: 900;
    color: var(--primary-blue);
    margin-bottom: 0.25rem;
  }
  .post-content-body :global(.stat-lbl) {
    font-size: 0.78rem;
    color: var(--text-muted);
    line-height: 1.35;
  }

  /* Smart Callout */
  .smart-tool-callout {
    background: var(--card-bg);
    border: 1px solid var(--card-border);
    border-radius: 16px;
    padding: 1.5rem;
    margin: 3rem 0 2rem;
    display: flex;
    align-items: center;
    gap: 1.25rem;
    flex-wrap: wrap;
  }
  .smart-tool-icon { font-size: 2.2rem; }
  .smart-tool-info { flex: 1; min-width: 240px; }
  .smart-tool-info h3 { font-size: 1.15rem; font-weight: 800; margin: 0 0 0.25rem; }
  .smart-tool-info p { font-size: 0.88rem; color: var(--text-muted); margin: 0; }
  .smart-tool-btn {
    background: var(--primary-blue);
    color: #ffffff;
    font-weight: 800;
    font-size: 0.88rem;
    padding: 0.65rem 1.15rem;
    border-radius: 10px;
    text-decoration: none;
    white-space: nowrap;
  }
<!-- Related Useful Guides (Reduces Bounce Rate & Multiplies Pageviews) -->
    <div class="related-guides-section notranslate" style="margin-top: 3rem; padding-top: 2rem; border-top: 1px dashed var(--card-border);">
      <h3 style="font-size: 1.25rem; font-weight: 800; margin-bottom: 1.25rem;">📌 Recommended Guides for You</h3>
      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 1rem;">
        <a href="/blog/cyprus-student-bus-card-discount-guide/" style="background: var(--card-bg); border: 1px solid var(--card-border); padding: 1.2rem; border-radius: 12px; text-decoration: none; color: inherit; display: block;">
          <span style="font-size: 0.75rem; color: var(--primary-blue); font-weight: 800; text-transform: uppercase;">Transit & Savings</span>
          <h4 style="margin: 0.4rem 0 0; font-size: 0.98rem; font-weight: 800;">How to Get a Cyprus Student Bus Card (50% Off) ➔</h4>
        </a>
        <a href="/blog/non-eu-student-part-time-work-cyprus-legal-guide/" style="background: var(--card-bg); border: 1px solid var(--card-border); padding: 1.2rem; border-radius: 12px; text-decoration: none; color: inherit; display: block;">
          <span style="font-size: 0.75rem; color: #059669; font-weight: 800; text-transform: uppercase;">Legal Work Rights</span>
          <h4 style="margin: 0.4rem 0 0; font-size: 0.98rem; font-weight: 800;">Non-EU Student Part-Time Work Rules & Minimum Wage ➔</h4>
        </a>
      </div>
    </div>
  /* Footer Section */
  .post-footer-section {
    border-top: 1px dashed var(--card-border);
    padding-top: 2rem;
    margin-top: 2rem;
  }
  .share-box-wrap {
    margin-bottom: 2rem;
  }
  .share-label {
    font-size: 0.9rem;
    font-weight: 700;
    color: var(--text-muted);
    display: block;
    margin-bottom: 0.75rem;
  }

  .back-hub-wrap {
    text-align: center;
    margin-top: 3rem;
  }
  .back-hub-link {
    display: inline-block;
    color: var(--text-muted);
    font-size: 0.92rem;
    font-weight: 700;
    text-decoration: none;
    border: 1px solid var(--card-border);
    padding: 0.5rem 1.25rem;
    border-radius: 30px;
    transition: all 0.15s ease;
  }
  .back-hub-link:hover {
    color: var(--primary-blue);
    border-color: var(--primary-blue);
  }
</style>
